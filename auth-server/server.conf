# Configuration file for the authentication server

#
# TCP port to reach the authentication server
ServerPort=5894

#
# Minimum time (milliseconds) between server update ticks
MinServerUpdateTime=1

#
# Time (in seconds) before the server is forced to crash if it is frozen
MaxCoreStuckTime=60

#
# Update server uptime period (in minutes)
UpdateUptimeInterval=10

#
# Update economy (gold, ap) period (in hours)
UpdateEconomyInterval=8
