package com.jftse.emulator.server.database.repository.player;

import com.jftse.emulator.server.database.model.player.BattlemonSlotEquipment;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BattlemonSlotEquipmentRepository extends JpaRepository<BattlemonSlotEquipment, Long> {
    // empty..
}
